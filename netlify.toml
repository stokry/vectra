[build]
  command = "mise install && mise exec -- bash -c 'gem install bundler && cd docs && bundle install && bundle exec jekyll build'"
  publish = "docs/_site"

[build.environment]
  JEKYLL_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

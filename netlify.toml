[build]
  command = "mise install ruby@3.2.0 && mise use ruby@3.2.0 && eval \"$(mise activate bash)\" && cd docs && bundle install && bundle exec jekyll build"
  publish = "docs/_site"

[build.environment]
  JEKYLL_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
